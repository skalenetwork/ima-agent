import { describe, beforeAll, test } from 'bun:test'

describe('browser module test', () => {
    beforeAll(() => {
        // add schain type
        // create validator, nodes & 3 chains
    })
    test('browse', () => {
        console.log('test')
    })
})
